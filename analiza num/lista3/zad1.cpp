#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int x = -20000;
    /*
        cout << "a)" << '\n';
        cout << 1 / (pow(x, 3) + sqrt(pow(x, 6) + pow(2023, 2))) << " ";
        cout << (sqrt(pow(x, 6) + 2023) - pow(x, 3)) / pow(2023, 2) << '\n';
    */

    /*
        double y = 3.999999999999;
        cout << "b)" << '\n';
        cout << (log2(y) - 2) << " ";
        cout << (log2(y / 4)) << '\n';
    */

    /*
    double z = 0.000001;
    cout << "c)" << '\n';
    cout << pow(z, -3) * (M_PI_2 - z - (M_PI_2 - atan(z))) << " ";
    cout << (atan(z) / pow(z, 3) - pow(z, -2)) << '\n';
    */
}